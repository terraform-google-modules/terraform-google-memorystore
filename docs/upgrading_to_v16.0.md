# Upgrading to v16.0

The v16.0 release contains backwards-incompatible changes.

## Minimum Provider Version
This update requires upgrading the terraform provider version to `7.15+` for redis cluster sub-module and `7.13+` for valkey sub-module.
